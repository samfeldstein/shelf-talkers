---
import FontFamily from "./FontFamily.vue";
import FontSize from "./FontSize.vue";
---

<fieldset>
  <legend>Font</legend>
  <!-- Reverts to default without this directive -->
  <FontFamily client:only="vue" />
  <FontSize client:load />
</fieldset>
